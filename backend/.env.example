# Copy to .env for local development only. In Azure, these are set via Key Vault-backed
# App Service configuration, never committed — see docs/16-assumptions-licensing-and-professional-advice.md.

BASECORE_ENVIRONMENT=development
BASECORE_DATABASE_URL=postgresql+psycopg://basecore:basecore@localhost:5432/basecore

BASECORE_ENTRA_TENANT_ID=
BASECORE_ENTRA_CLIENT_ID=

BASECORE_OS_DATA_HUB_API_KEY=
BASECORE_WHAT3WORDS_API_KEY=
